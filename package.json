{
  "name": "tet-prep-monolith",
  "version": "1.0.0",
  "description": "TET Preparation Platform",
  "scripts": {
    "start": "node server/index.js",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd client && npm install --include=dev && cd ../server && npm install",
    "build": "cd client && npm install --include=dev && npm run build",
    "render-postbuild": "cd client && npm install --include=dev && npm run build && cd ../server && npm install"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "bcryptjs": "^2.4.3",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0",
    "node-cron": "^4.2.1",
    "pdf-parse": "^1.1.1",
    "razorpay": "^2.9.6",
    "uuid": "^9.0.1"
  }
}
